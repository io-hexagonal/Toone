---
title: "Browser sessions"
navTitle: "Browser sessions"
description: "Bring authenticated browser context into Toone so agents can work with services you already use."
eyebrow: "Feature · Browser work"
pageType: "feature"
---

## Work beyond the chat window

Many useful processes depend on websites rather than APIs. Browser sessions give agents a way to work through those interfaces while keeping the browser context connected to the project.

## Use an authenticated session

Bring a session from your default browser into Toone when a task depends on an account you have already signed into. This avoids treating every browser task as a fresh, disconnected environment.

## Keep the context visible

Browser work should remain understandable to the person responsible for the project. Follow which session and page are involved, then intervene when a step requires judgment, approval, or corrected input.

## Use deliberate boundaries

Connect only the browser context needed for the task. An authenticated session can expose sensitive capabilities, so its scope should match the agent’s role and the outcome you asked it to produce.

Next: [Routines](/how-to/features/routines).
